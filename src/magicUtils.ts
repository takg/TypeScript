export function castSpell(spell: string): string {
    return `Casting ${spell} spell! 🔮`;
}

export function brewPotion(potion: string): string {
    return `Brewing ${potion} potion! 🧪`;
}

export const schoolName = "Hogwarts School of Witchcraft and Wizardry";
